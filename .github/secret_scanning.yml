# Codeceipt test corpus contains DELIBERATELY secret-shaped strings — they are
# fabricated fixtures used to prove the engine's secret-detection actually fires.
# None are live credentials. Exclude them from GitHub secret scanning so the
# product's own test data doesn't raise false-positive alerts.
paths-ignore:
  - "fixtures/**"
  - "**/*.test.ts"
  - "**/*.spec.ts"
